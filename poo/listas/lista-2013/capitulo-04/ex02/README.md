# Exercício 02 – Lâmpada

Este exercício implementa a classe `Lampada` com um construtor obrigatório que define o estado inicial da lâmpada.

## 📌 Objetivo
Garantir que toda instância da classe seja criada com um estado inicial válido.

## 🧱 Estrutura da Classe
A classe possui:
- Um atributo privado `acesa` do tipo boolean
- Um construtor que exige a definição do estado inicial
- Métodos de acesso (`get` e `set`)
- Um método para exibir o estado da lâmpada no console

## 💡 Funcionamento
- `true` → lâmpada acesa
- `false` → lâmpada apagada

O método `mostrarEstado()` informa o estado atual da lâmpada.
