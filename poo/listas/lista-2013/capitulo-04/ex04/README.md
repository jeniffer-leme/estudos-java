# Exercício 04 – Classe Contador

Este exercício trabalha a criação de construtores sobrecarregados para uma classe que representa um contador simples.

## 📌 Objetivo
Praticar:
- Construtores com e sem argumentos
- Encapsulamento
- Manipulação de estado interno de um objeto

## 🧱 Estrutura da Classe
A classe `Contador` possui:
- Um atributo privado `contador` (int)

## ⚙️ Construtores
- `Contador()`: inicializa o contador com valor 0
- `Contador(int contador)`: inicializa o contador com o valor informado

## 🔁 Métodos
- `incrementarValor()`: incrementa o contador em 1
- `zerarValor()`: redefine o contador para 0
- `imprimirValor()`: imprime o valor atual do contador no console

## 📝 Observação
A classe permite iniciar a contagem tanto a partir de zero quanto a partir de um valor inicial definido pelo usuário.
